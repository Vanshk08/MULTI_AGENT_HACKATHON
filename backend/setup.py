from setuptools import setup, find_packages

setup(
    name="agentflow_backend",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies will be installed from requirements.txt
    ],
    python_requires=">=3.8",
)
